# Matches the Prettier configuration, so an editor agrees with it before it runs.
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.{ts,mts,js,mjs,cjs}]
max_line_length = 180
